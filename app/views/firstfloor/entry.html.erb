<div id='package_div'>
  <%=image_tag ('package_highlight.png'), id: 'package_highlight' if true%> <!-- use the conditional to test if the user has already collected the package -->
  <%=image_tag ('package.png'), id: 'package' if true%> <!-- use the conditional to test if the user has already collected the package -->
</div>

<%=image_tag ('entryway.jpg'), id: 'entry'%>